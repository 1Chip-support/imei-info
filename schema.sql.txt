-- schema.sql
CREATE TABLE CHECKS (
    DEVICEID VARCHAR2(50) PRIMARY KEY,
    EMAIL VARCHAR2(255),
    TYPE VARCHAR2(50),
    STATUS VARCHAR2(50) DEFAULT 'pending',
    PRICE NUMBER DEFAULT 1.99,
    PAID NUMBER(1) DEFAULT 0,
    TIME TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);